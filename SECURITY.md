# 安全报告

如果仓库的 **Security** 页面提供 **Report a vulnerability**，请通过该入口提交私密漏洞报告。若入口尚未启用，请先创建仅包含“请求私密安全报告渠道”的 Issue，等待维护者提供私密途径。不要在公开 Issue 中披露尚未修复的凭据泄露、任意命令执行或数据丢失复现细节。

报告中请说明受影响版本、系统环境、复现条件和预期影响。尽量使用临时仓库和虚构数据；不要附上真实令牌、SSH 私钥或业务代码。

## 当前边界

- 当前维护目标为 main 分支和最新版本，不承诺长期维护旧版本。
- GitSprig 调用系统 Git，并沿用其凭据助手、SSH、钩子和签名配置。
- AI 仅在用户点击生成时，将所选差异及必要上下文发送到用户配置的服务。
- AI 配置和 API Key 以明文保存在用户目录的 .gitgui/ai.json；macOS 上目录权限为 700、文件权限为 600。
- 自行构建的默认安装包使用临时签名，未经过 Apple 公证。开源许可证与 Apple 签名、公证属于不同环节。
